language: rust
rust:
  - stable
  - beta
  - nightly
os:
  - linux
  - osx

cache:
  cargo: true

matrix:
  allow_failures:
    - rust: nightly

addons:
  apt:
    packages:
      - libsodium-dev
    sources:
      - sourceline: ppa:chris-lea/libsodium
